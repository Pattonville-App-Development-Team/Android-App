<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?android:selectableItemBackground">

    <TextView
        android:id="@+id/directory_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="8dp"
        app:layout_constraintBottom_toBottomOf="@+id/directory_icon"
        app:layout_constraintLeft_toRightOf="@+id/directory_icon"
        app:layout_constraintTop_toTopOf="@+id/directory_icon"
        tools:text="School Name Here" />

    <ImageView
        android:id="@+id/directory_icon"
        android:layout_width="75dp"
        android:layout_height="75dp"
        android:padding="8dp"
        tools:ignore="ContentDescription"
        tools:src="@drawable/d_mascot" />
</android.support.constraint.ConstraintLayout>